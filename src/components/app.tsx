import { h, FunctionComponent } from 'preact';

interface App {

}

const app: FunctionComponent<App> = () => {
    return (
        <div>
            Hello from Preact
        </div>
    );
};

export default app;
